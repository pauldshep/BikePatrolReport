<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.sss.bikepatrolreport">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_patrol"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round_patrol"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".TimerActivity"></activity>
        <activity android:name=".TrailSelectionActivity" />
        <activity android:name=".PatrolLocationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
